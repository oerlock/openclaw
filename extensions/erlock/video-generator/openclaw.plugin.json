{
  "id": "video-generator",
  "name": "Video Generator",
  "description": "DashScope Wan video generation plugin.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "DashScope API key from pluginConfig.apiKey. Optional at install time; required before tool calls."
      },
      "createTaskUrl": {
        "type": "string",
        "description": "Optional override for async task creation URL from pluginConfig.createTaskUrl; defaults to DashScope video synthesis endpoint."
      },
      "taskResultBaseUrl": {
        "type": "string",
        "description": "Optional override for task result base URL from pluginConfig.taskResultBaseUrl; defaults to https://dashscope.aliyuncs.com/api/v1/tasks."
      }
    }
  }
}
