{
  "id": "image-generator",
  "name": "Image Generator",
  "description": "DashScope Qwen image generation and editing plugin.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "DashScope API key from pluginConfig.apiKey."
      },
      "baseUrl": {
        "type": "string",
        "description": "Override API endpoint from pluginConfig.baseUrl; defaults to the built-in DashScope multimodal generation endpoint."
      }
    }
  }
}
